import React from "react";
import {Typography} from "@material-ui/core"

const Klavierlehrerin = () => {
  return (
    <div>
      <Typography variant="h6">
        Klavierlehrerin
      </Typography>
    </div>
  );
};

export default Klavierlehrerin;